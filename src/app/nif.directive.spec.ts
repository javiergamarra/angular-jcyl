import {NIFDirective} from './cif.directive';

describe('NIFDirective', () => {
  it('should create an instance', () => {
    const directive = new NIFDirective();
    expect(directive).toBeTruthy();
  });
});
